<?php
$hostname = "localhost";
$username = "root";
$password = "";
$database = "employee_id_card";

// Create a connection
$connection = mysqli_connect($hostname, $username, $password, $database);

// Die if connection was not successful
?>